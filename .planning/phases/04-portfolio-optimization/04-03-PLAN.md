---
phase: 04-portfolio-optimization
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06
must_haves:
  truths:
    - "User can type custom weight percentages and remaining holdings rebalance to 100%"
    - "Sector donut chart shows colored segments for Banking/Insurance/Standard"
    - "3 risk metric cards display Sortino, Max Drawdown, CVaR values"
    - "Efficient frontier scatter plot shows grey dots and green current portfolio dot"
    - "Optimization table shows current vs optimal weights with arrows"
    - "Apply Optimal Weights button updates holdings"
    - "Analysis tab placeholder appears when fewer than 2 holdings have price data"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the complete portfolio optimization feature end-to-end in a running browser.

Purpose: Confirm that all PORT-01 through PORT-06 requirements work correctly in the live Reflex app — UI renders, charts display, weight input rebalances, optimization runs.
Output: Human-verified confirmation that the portfolio page is fully functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-portfolio-optimization/04-CONTEXT.md
@.planning/phases/04-portfolio-optimization/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end portfolio optimization verification</name>
  <files>financial_dashboard/pages/portfolio.py</files>
  <read_first>
    - .planning/phases/04-portfolio-optimization/04-CONTEXT.md
  </read_first>
  <action>
Start the Reflex app with `reflex run` if not already running. Guide user through verification of all PORT-01 through PORT-06 requirements in the browser at http://localhost:3000/portfolio.

What was built:
- Holdings tab with inline weight inputs and proportional rebalancing
- Analysis tab with sector donut chart, 3 risk metric cards, efficient frontier scatter plot, and optimization table
- Apply Optimal Weights button that updates all holdings
- Conditional rendering: placeholder shown when fewer than 2 companies have price data
  </action>
  <verify>
    <automated>cd "/Users/dlgvnbyr/Documents/Hicheel/Capstone Project/financial-dashboard" && python -m pytest tests/ -x 2>&amp;1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed all 6 PORT requirements work correctly in the browser. No runtime errors.</done>
  <how-to-verify>
Start the app if not running: `reflex run` from project root.

**Holdings Tab (PORT-01):**
1. Navigate to http://localhost:3000/portfolio
2. If empty, go to Screener and add at least 3 companies (ideally ones with price data: any of the 7 demo companies)
3. Verify the Holdings tab is active by default with inline weight inputs (number fields, not static text)
4. Type a new weight (e.g., "50") into one holding's input field, then click outside (blur)
5. Verify the other holdings' weights update proportionally and all weights sum to 100%
6. Verify the blended health score header still displays correctly

**Analysis Tab — Placeholder (PORT-03/04/05/06 guard):**
7. Remove companies until only 1 remains. Switch to Analysis tab.
8. Verify placeholder message: "Add at least 2 companies with price history to see portfolio analysis."

**Analysis Tab — Full (PORT-02 through PORT-06):**
9. Add back at least 2 demo companies with price data (e.g., any from the 7 seeded companies)
10. Switch to the Analysis tab
11. Verify sector donut chart appears (left side) with colored segments
12. Verify 3 risk metric cards appear (right side): Sortino Ratio, Max Drawdown, CVaR (95%) — values should be numbers, not "N/A"
13. Verify efficient frontier scatter plot appears below — grey dots forming a cloud, with a green dot for the current portfolio
14. Verify optimization table at the bottom: Company | Current | Optimal columns, with directional arrows
15. Click "Apply Optimal Weights" button
16. Switch to Holdings tab — verify weights updated to the optimal values
17. Switch back to Analysis tab — verify all charts/metrics recalculated with new weights
  </how-to-verify>
  <acceptance_criteria>
    - Holdings tab renders with inline weight inputs (not static text)
    - Weight rebalance works on blur: other holdings adjust proportionally, sum = 100%
    - Analysis tab placeholder shown with fewer than 2 holdings with price data
    - Sector donut chart visible with at least one colored segment
    - All 3 risk metric cards show numeric values (not N/A) when 2+ companies have price data
    - Efficient frontier scatter plot visible with grey dots and green current portfolio dot
    - Optimization table shows current and optimal weights with arrows
    - Apply Optimal Weights button updates holdings weights
    - No console errors in browser dev tools
    - No Python traceback in reflex server output
  </acceptance_criteria>
  <resume-signal>Type "approved" if everything works, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual and interactive verification of the complete portfolio optimization feature.
</verification>

<success_criteria>
- All PORT-01 through PORT-06 requirements visually confirmed working in browser
- No runtime errors in browser console or server logs
- Weight rebalancing, charts, optimization, and Apply button all functional
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-optimization/04-03-SUMMARY.md`
</output>
